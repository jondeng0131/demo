import React from "react";
import ReactDOM from "react-dom/client";
import App from "./mediminder-pro.jsx";  // ← match your actual filename

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
